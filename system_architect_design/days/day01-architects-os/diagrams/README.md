# diagrams/ — Day 1

Put your C4 `.mmd` diagrams here (see `reference/c4-guide.md`).

Expected today: `context.mmd` (C1) and `containers.mmd` (C2) for the system you
reverse-engineered. Starter skeletons are in `../lab/`.
