# design/ — Day 1

Put your filled-in 7-step design here (see `reference/design-method.md`).

Expected today: `reverse-engineer.md` — a real system you operate, run through the
method as if designing it fresh (requirements → constraints → top-3 NFRs → options
→ tradeoff table → decision → 5 ways it breaks).
