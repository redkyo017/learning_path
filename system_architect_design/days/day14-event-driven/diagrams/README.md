Put your C4 `.mmd` diagrams here (see `reference/c4-guide.md`). Today: a C2 showing
Orders → `orders` topic → two independent consumer groups (Inventory, Analytics),
each with its own offset, plus the dead-letter topic.
