Put your filled-in 7-step design here (see `reference/design-method.md`). Today:
the `orders` topic design — payload style (notification vs ECST), partition key,
partition count, retention — with a tradeoff table and a self-red-team (poison
message, commit-before-process, retention-too-short-to-bootstrap).
