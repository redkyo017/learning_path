Put your ADR(s) here as `NNNN-title.md` (see `reference/adr-template.md`). ADR
numbers are global across all days. Today (suggested `0014`): event notification vs
event-carried state transfer for `OrderPlaced`, plus the topic key + partition-count
decision.
