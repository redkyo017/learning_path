# Day 20 — design

Put your filled-in 7-step design framing here (see `reference/design-method.md`):
which prior design you chose to threat-model + cost-model, its trust boundaries, and
why. The STRIDE table lives in `../lab/stride-worksheet.md`; the cost model in
`../lab/cost-model.md`.
