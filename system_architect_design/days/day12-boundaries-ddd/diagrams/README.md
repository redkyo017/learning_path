# diagrams/ — Day 12

Put your C4 `.mmd` diagrams here (see `reference/c4-guide.md`). For today: a
context-map diagram (a `flowchart` is fine) showing the bounded contexts, their
aggregate roots, and the edges labelled sync/async + relationship (e.g. ACL to a
payment vendor). Optionally a C2 showing the modular-monolith module boundaries.
