# diagrams/ — Day 10

Put your C4 `.mmd` diagrams here (see `reference/c4-guide.md`). For today: a C2
container diagram showing the thin router in front of N independent cells (each
cell = LB + services + DB + cache, sharing nothing). Optionally a second diagram
contrasting single-region-multi-cell vs multi-region.
