version: v2
# Local plugins (installed via `go install` in Beat 0) so this works fully offline.
plugins:
  - local: protoc-gen-go
    out: gen
    opt: paths=source_relative
  - local: protoc-gen-go-grpc
    out: gen
    opt: paths=source_relative
