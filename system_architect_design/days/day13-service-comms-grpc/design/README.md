Put your filled-in 7-step design here (see `reference/design-method.md`). Today:
assign each of the Orders ↔ Inventory ↔ Payments edges sync/async + a protocol, with
a tradeoff table and a self-red-team of a breaking schema change.
