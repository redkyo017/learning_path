# design/ — Day 11

Put your filled-in 7-step design here (see `reference/design-method.md`). For
today: the observability design — your 3–5 **SLIs** (as good/total ratios), their
**SLO targets** and window, the **error-budget policy** (what happens when it
burns), the trace-context propagation plan, and the RED/USE metric set. Red-team
every SLI: can you compute it from what you emit?
