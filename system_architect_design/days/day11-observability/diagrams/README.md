# diagrams/ — Day 11

Put your C4 `.mmd` diagrams here (see `reference/c4-guide.md`). For today: a
sequence or C2 diagram showing a request's **trace-context flow** across services
(A→B), where spans start/end, and where telemetry is exported (OTel → Jaeger /
Prometheus).
