# adr/ — Day 11

Put your ADR(s) here as `NNNN-title.md` (see `reference/adr-template.md`). ADR
numbers are **global across all days**. Today's suggested ADR (0011): the chosen
SLIs/SLOs + error-budget policy and the observability approach (RED metrics on
100% of traffic, propagated + sampled traces, alert on SLO burn rate).
