# Day 19 — diagrams

Put your C4 `.mmd` diagrams here (see `reference/c4-guide.md`). A plan→act→observe
sequence diagram and a C2 container view (agent runtime, tool gateway, retriever,
guardrails) are the useful ones for an agent.
