# Day 19 — design

Put your filled-in 7-step design here (see `reference/design-method.md`): the
tool-using agent with guardrails, its top-3 NFRs, ≥2 options (single-agent vs.
multi-agent), the tradeoff table, the decision, and the red-team (prompt injection).
