Put your C4 `.mmd` diagrams here (see `reference/c4-guide.md`). Today: a C2 showing
the CQRS split — Command → Order aggregate → append-only event store → projector →
read model(s) → Query — with the write and read sides clearly separated.
