Put your filled-in 7-step design here (see `reference/design-method.md`).
Today: A protected from a failing B — requirements, top-3 NFRs, ≥2 options
(timeout-only vs timeout+breaker vs timeout+breaker+bulkhead+fallback), tradeoff
table, decision, red-team (half-open stampede, fallback-calls-a-down-service).
