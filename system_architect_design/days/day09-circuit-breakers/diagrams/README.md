Put your C4 .mmd diagrams here (see `reference/c4-guide.md`).
Today: the circuit-breaker state machine (closed/open/half-open) and/or the
A → bulkhead → breaker → B (else cached fallback) request flow.
