# diagrams/ — Day 2

Put your C4 `.mmd` diagrams here (see `reference/c4-guide.md`).

Expected today (optional): a C2 sketch of the shortener showing where the estimate
lands each component (cache for hot reads, shards for storage, queue for write peaks).
