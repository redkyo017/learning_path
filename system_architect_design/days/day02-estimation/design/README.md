# design/ — Day 2

Put your filled-in 7-step design here (see `reference/design-method.md`).

Expected today: `sizing.md` — the URL-shortener capacity estimate (write/read QPS
avg + peak, 5-yr storage, egress bandwidth, cache working set) plus ≥2 ID-scheme
options with a tradeoff table. Red-team: what breaks at 10×?
