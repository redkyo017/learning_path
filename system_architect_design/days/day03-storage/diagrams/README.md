# diagrams/ — Day 3

Put your C4 `.mmd` diagrams here (see `reference/c4-guide.md`).

Expected today (optional): a C2 diagram showing the chosen store(s) on the read vs
write path, plus a small data-model sketch (table/keys or item collection).
