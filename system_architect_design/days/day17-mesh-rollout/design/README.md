Put your filled-in 7-step design here (see `reference/design-method.md`).

Today: the v1 → v2 Orders rollout. Include the rolling vs blue-green vs canary
tradeoff table, the exact automated-rollback trigger (metric / threshold /
window, tied to the Day-11 SLIs), and the red-team (healthy-canary-corrupts-data,
session affinity, absolute-vs-relative threshold). Save your Problem-3 interview
attempt here too.
