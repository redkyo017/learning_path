Put your C4 .mmd diagrams here (see `reference/c4-guide.md`).

Suggested for today: a container/flow diagram with the offline ingest path
(docs → chunk → embed → pgvector) and the online query path (question → embed →
retrieve top-k → prompt → LLM → answer), plus the semantic cache short-circuit.
