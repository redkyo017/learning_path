# security.protocol and sasl.mechanisms are set in code by
# kafkaclient.MaybeConfigureAWSIAM based on auth.mode below — don't set them
# here too, that would just create two conflicting sources of truth.
bootstrap.servers=<BootstrapBrokerStringSaslIam>
auth.mode=aws-msk-iam
aws.region=<AWS_REGION>
