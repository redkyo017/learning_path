# Day 11 findings
## Correctness
- [11-C1] CRITICAL — content/day11.md:302 — Solution 4 gives the $k=4$ check as "$\sin^2(146.15°)$... using the coterminal angle, $\approx0.582$" — the angle is wrong: $(2\cdot4+1)\theta/2 = 4.5\theta = 4.5\times28.955° = 130.30°$ (= 2.2741 rad, matching Solution 5's own table), not 146.15°; and $\sin^2(146.15°)=0.310\ne0.582$, so the printed angle contradicts the printed value (0.582 is correct for 130.30°); "coterminal" is also the wrong term (nothing needs reduction at 130.30°) — replace with $\sin^2(130.30°)\approx0.582$.
- [11-C2] MINOR — content/day11.md:315-327 — Solution 5's $P(k)$ table has several entries off in the 3rd–4th decimal: exact values (re-derived from $\sin(\theta/2)=1/4$ via multiple-angle identities, e.g. $\sin(3\theta/2)=3s-4s^3=0.6875$ exactly) are $P(1)=0.4727$ (table: 0.4729), $P(2)=0.9084$ (0.9076), $P(3)=0.9613$ (0.9616), $P(7)=0.3649$ (0.3657), $P(10)=0.6869$ (0.6849); the table's $P(2)=0.9076$ also contradicts Solution 4's own "$\sin^2(72.39°)\approx0.908$" two paragraphs earlier, and the code lab prints 4 decimals, so a learner comparing digit-for-digit will see mismatches (the code's output is the correct one) — recompute the table; all qualitative conclusions (peak $k=3$, trough $k=6$, second peak $k=9$) are unaffected.
- [11-C3] MINOR — content/day11.md:297-299 — Solution 4: "$\frac{3.14159}{1.01072}\approx3.1089$" should be 3.1083 (so $k^\star\approx2.6083$, not 2.6089), and "$\approx101.36°$" for 1.7688 rad should be 101.34° — both round to the same answer ($k=3$), pure arithmetic slips.
- [11-C4] MINOR — content/day11.md:37-38 — "an oracle over $N=2^n$ items, $n$ of which are indices into a list with no exploitable structure" — garbled sentence: the $N$ items are indexed by $n$-bit strings; "$n$ of which are indices" is not a meaningful claim — rewrite, e.g. "an oracle over $N=2^n$ items (indexed by $n$-bit strings) with no exploitable structure".
## Consistency
- [11-S1] MINOR — content/day11.md:368-370 — Code lab expected behavior says probability will "*decrease* again on iterations 4 and beyond", but the code runs to iteration 10 and its output rises again from $k=7$ to a second peak of 0.9922 at $k=9$ — exactly the periodic behavior of Exercise 5's table that the same sentence cites — reword to "decrease on iterations 4–6, then climb again toward a second peak near $k=9$".
## Prerequisites
- none
## Time budget
- estimated hours: 3.5–5 h — 385 lines; theory is geometric and self-contained (unitarity/reflection proofs, one $2\times2$ matrix product), 5 exercises of moderate depth plus a short run-and-record code lab; assumes a learner comfortable with linear algebra (2D reflections/rotations taken as known, e.g. line 157-159's reflection matrix is cited, not derived) but new to quantum.
## Code lab
- [11-L1] MINOR — content/day11.md:376-377 — learner is told to record results in `notes/day11_grovers.md`, but no `notes/` directory exists in the repo (only `code/`, `content/`, `docs/`); the plan file makes the same reference — either create the directory or say "create `notes/`" explicitly.
- [11-L2] MINOR — code/day11_grover_simulation.py:1-23 — static check otherwise clean: imports (numpy only) and logic match the content's description exactly — `oracle()` flips the sign at the marked index ($I-2|x_m\rangle\langle x_m|$), `diffusion()` computes `2*np.vdot(s,state)*s - state` ($D=2|s\rangle\langle s|-I$), iteration order oracle-then-diffusion matches $D\cdot O_f$, prints $P$ for iterations 0–10 with $P(0)=0.0625$ and peak $\approx0.9613$ at $k=3$ as claimed by content/day11.md:364-371 and README.md:10-12, and the listing is byte-identical to the plan's Day 11 code block — the only output mismatch a learner will hit is Exercise 5's slightly-wrong table values (see 11-C2; the code is right).
## Primer-ability
- citable labels: partial — no bold named labels (confirmed vs inventory), but the ### section names are clean, citable anchors: "The oracle reflection $O_f$" (line 78), "The diffusion operator $D$" (line 99), "Composing two reflections: Grover's algorithm as rotation" (line 113), "How many iterations?" (line 130).
- hook candidate: "Take $N=4$ ($n=2$ qubits), $M=1$. Then $\sin(\theta/2)=\sqrt{1/4}=1/2$, so $\theta/2=30°$ and $\theta=60°=\pi/3$" (line 149-151) — the fully numeric exact-Grover instance where one iteration rotates $|s\rangle=(0.8660,0.5)$ to exactly $(0,1)=|good\rangle$ (lines 175-181), success probability exactly 1.
