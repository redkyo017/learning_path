# Day 07 findings
## Correctness
- [07-C1] MINOR — content/day07.md:226 — "No linear map can implement a quadratic one on more than a one-dimensional set of inputs" — imprecise as stated: a linear map CAN agree with the cloning action on two orthogonal states (CNOT clones both $|0\rangle$ and $|1\rangle$ exactly, and their span is 2-dimensional); the true obstruction is that no linear map can clone any two *non-orthogonal* states (equivalently, cannot also clone any superposition of already-cloned states) — reword to "no linear map can agree with the cloning map on two non-orthogonal states, hence never on a state and a superposition involving it", matching the $|0\rangle,|1\rangle,|+\rangle$ structure of the proof above it. All formal content of the day checks out: basis vectors, Kronecker block rule, mixed-product/adjoint rules and the unitarity argument (line 74–84), CNOT matrix vs. its truth table (line 87–92), Bell construction (line 109–121), partial-trace definition and $\rho_1=I/2$ computation (line 349–388), no-cloning proof with the $0$ vs $\tfrac12$ and $\tfrac{1}{\sqrt2}$ vs $\tfrac12$ comparison (line 204–219), all four Bell-state derivations in the worked example (line 241–252, verified term by term), and the $X\otimes Z$ matrix and its $M^2=I_4$ row-by-row check (line 427–441).
## Consistency
- none (exercises 1–7 each have a matching solution that solves the stated problem; internal cross-references "Exercise 3/Solution 3" and "Exercise 4/Solution 4" resolve; notation for kets, $\otimes$, and $\text{Tr}_2$ is uniform throughout)
## Prerequisites
- none (tensor product, Kronecker product, partial trace, separability, and no-cloning are all introduced in this file; density matrices, pure vs. mixed, and the rank-1 purity signature used at lines 150–158 and 452–468 are introduced on Day 6 per inventory.md, day06.md:13–14, 89–99; $H$, $X$, $Z$ and unitarity/adjoints come from Days 3–4)
## Time budget
- estimated hours: 5–7 h — ~470 dense lines with two full proofs to internalize (no-cloning, partial trace) plus 7 exercises of which two are complete proof reproductions, one an explicit partial-trace computation, and one a $4\times4$ verification; assumes a learner comfortable with linear algebra but new to quantum.
## Code lab
- not applicable
## Primer-ability
- citable labels: yes — bold **Claim:** for the no-cloning theorem (line 162) and for the worked example "same two-gate circuit produces all four Bell states" (line 232), plus clean `###` anchors: "Embedding single-qubit gates: the Kronecker product", "Separability and entanglement", "The partial trace and reduced density matrices", "The no-cloning theorem".
- hook candidate: the no-cloning numeric clash at lines 204–213 — "$\tfrac{1}{\sqrt2}(|00\rangle+|11\rangle)$ (actual, from linearity) vs. $\tfrac12(|00\rangle+|01\rangle+|10\rangle+|11\rangle)$ (required, from the cloning assumption) … The coefficient of $|01\rangle$ is $0$ on the left and $\tfrac12$ on the right; the coefficient of $|00\rangle$ is $\tfrac{1}{\sqrt2}\approx0.707$ on the left and $\tfrac12=0.5$ on the right."
