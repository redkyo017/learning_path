# Day 04 findings

## Correctness
- [04-C1] MINOR — content/day04.md:219 — "A single-qubit unitary acts on the Bloch sphere as a rigid rotation … (it must preserve $x^2+y^2+z^2=1$ and, being linear, preserve the geometric structure of great circles)" presented as "immediate from the coordinate formula" — the fact is true, but the parenthetical is not a proof: preserving the unit sphere plus linearity does not by itself rule out improper (reflection-type) isometries; the rotation (not reflection) conclusion needs the U(2)→SO(3) homomorphism / connectedness argument — reword as "we assert without proof" or drop the pseudo-justification.
- [04-C2] MINOR — content/day04.md:284 — entry (2,2) of the reconstruction contains garbled abandoned derivation text ("$(-1+i)(-i)=i-i^2\cdot... $ — expand directly: $(-1+i)(-i)=i-i\cdot i\cdot(-1)$; more carefully …") — two false starts were left in the published text before the correct expansion $(-1+i)(-i)=i-i^2=1+i$; the final value (entry $=1$) is correct — delete the false starts, keep only the correct expansion. (All other Day 4 math re-derived and verified: spectral-theorem both directions, triangular-normal lemma induction, worked example $A=[[1,1],[-1,1]]$ with $\lambda=1\pm i$ and $UDU^\dagger=A$, all seven solutions, Bloch derivation.)

## Consistency
- [04-S1] MINOR — content/day04.md:473 — instructs writing to `notes/day04_normal_matrices_bloch.md`, but no `notes/` directory exists in the repo — learner must infer they should create it — add a one-line "create `notes/` on Day 1" note or scaffold the directory. (Other cross-references verified: plan file `docs/superpowers/plans/2026-07-13-quantum-computing-15-day-plan.md` exists and contains the Step 4.6 reference and the exact code listing; `code/day04_bloch_sphere.py` exists; Day 3 facts cited (adjoint, unitary, modulus-1 eigenvalues) are present per inventory. Exercises 1–7 each have a matching solution that solves the stated problem.)

## Prerequisites
- [04-P1] MINOR — content/day04.md:70 — Schur's theorem is cited without proof as the one load-bearing external fact in the hard direction — the file flags this honestly, and it is acceptable for a learner comfortable with linear algebra, but it is the only unproven ingredient in an otherwise "prove both directions" day — add a textbook pointer (e.g. Horn & Johnson §2.3) for learners who want the proof. (Characteristic polynomial via trace/determinant, Gram–Schmidt, and double-angle identities are assumed LA/trig background, consistent with the stated learner profile.)

## Time budget
- estimated hours: 4.5–6 h — 481 lines with high proof density (Schur-based spectral proof, triangular-normal induction lemma, full worked spectral decomposition, 7 exercises including one general algebraic proof, plus code lab and notes), assuming a learner comfortable with linear algebra but new to quantum.

## Code lab
- [04-L1] MINOR — content/day04.md:467 — claims expected output "`H|0>` at `(1,0,0)` — matching your Exercise 5 by-hand computation exactly" — the script prints raw floats and $x=2(1/\sqrt2)^2$ evaluates to 1.0000000000000002 in IEEE arithmetic, so the printed line will not literally read `(1,0,0)` — soften "exactly" to "up to float round-off". (Otherwise the lab checks out statically: only `numpy` imported; `bloch_coords` implements exactly the Theory-section formulas; it prints the five states the content and README claim, in order; the $\psi=(0.6,0.8i)$ pair gives $(0,0.96,-0.28)\to(0,-0.96,0.28)$, confirming the advertised $x$ fixed / $y,z$ flipped behavior; README's "runnable as-is, expected output documented in content and plan" is accurate.)

## Primer-ability
- citable labels: partial — one bold label (`**Claim:**` line 233); otherwise strong `###` names ("Normal matrices and the spectral theorem", "Hermitian matrices are normal, with real eigenvalues", "The Pauli matrices and the Hadamard matrix", "The Bloch sphere") plus numbered exercises.
- hook candidate: the worked example, line 233 — "**Claim:** $A = \begin{pmatrix}1&1\\-1&1\end{pmatrix}$ is normal, and its spectral decomposition can be found and verified explicitly" — with concrete numbers $AA^\dagger=A^\dagger A=2I$, eigenvalues $1\pm i$, $|1\pm i|=\sqrt2$; runner-up: code-lab state $|\psi\rangle=(0.6,\,0.8i)$ mapping $(0,0.96,-0.28)\to(0,-0.96,0.28)$ under $X$ (line 458).
