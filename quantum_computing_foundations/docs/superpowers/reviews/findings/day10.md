# Day 10 findings
## Correctness
- [10-C1] MINOR — content/day10.md:6 — objective states $H^{\otimes n}\sum_x(-1)^{a\cdot x}|x\rangle = |a\rangle$ — missing the $\frac{1}{\sqrt{2^n}}$ normalization: as written the LHS has norm $\sqrt{2^n}$, not 1 (the body, e.g. lines 104–116, gets it right) — add the $\frac{1}{\sqrt{2^n}}$ prefactor to the objective.
- [10-C2] MINOR — content/day10.md:147 — "A classical algorithm learns nothing about $s$ from any query that doesn't land on both members of some colliding pair" — overstated: two distinct queries $x,x'$ with $f(x)\ne f(x')$ do reveal information, namely $s\ne x\oplus x'$ (candidate elimination is exactly what the rigorous $\Omega(2^{n/2})$ lower-bound proof counts); the same overstatement recurs in Solution 3 (line 383–384, "detectable classically only by observing an actual collision") — soften to "learns almost nothing — only that $s\ne x\oplus x'$ for each queried non-colliding pair"; the $\Omega(2^{n/2})$ conclusion is unaffected and correct.
## Consistency
- [10-S1] MINOR — content/day10.md:99-101 — "this is the 'use the identity twice' referred to in the learning objectives" — no learning objective contains that phrase; objective 1 (line 6-9) just says to derive from the Hadamard-transform identity — either add the phrase to the objective or drop the back-reference.
- [10-S2] MINOR — content/day10.md:246 vs 320-326 — bit-index convention drifts within the file: the BV worked example and Solution 2 index bits as $x_2x_1x_0$ (0-indexed from the right), while Exercise 5 / Solution 5 use $s'=(s_1,s_2,s_3,s_4)$ (1-indexed from the left) — each is locally self-consistent and the math is correct, but a one-line remark on the convention switch would prevent confusion.
## Prerequisites
- [10-P1] MAJOR — content/day10.md:176-185 — Simon's derivation measures only the second register of an entangled joint state and invokes projection-onto-surviving-branches + renormalization — the measurement postulate as introduced (day06.md lines 35-46) covers only full-system measurement in an orthonormal basis (collapse to a single basis vector), and days 7–9 never extend it to subsystem measurement; day10 does compute the renormalization explicitly inline, which mitigates, but the rule itself is used without ever being stated as a postulate — add 1–2 sentences (here or in day06/day07) stating the partial-measurement rule: project onto the subspace consistent with the observed outcome, renormalize.
## Time budget
- estimated hours: 4.5–6 h — 493 lines with two full algorithm derivations (BV exact, Simon collapse + constraint), a state-by-state trace, and 6 exercises including a from-scratch re-derivation and a fully worked $\mathbb{F}_2$ Gaussian elimination; assumes a learner comfortable with linear algebra but new to quantum (all derivations verified correct, so no debugging overhead).
## Code lab
- not applicable
## Primer-ability
- citable labels: partial — no bold named labels (confirmed vs inventory), but the ### section names are distinctive, citable anchors: "The parity-orthogonality lemma" (line 58), "Bernstein–Vazirani: statement and general derivation" (line 74), "Simon's circuit: collapse and the linear constraint on $y$" (line 161), "Unifying view" (line 213).
- hook candidate: "**Bernstein–Vazirani for $n=3$, $a=101$ (i.e. $a_2a_1a_0 = 1,0,1$), traced state by state.**" (line 243) — comes with the complete 8-row $(-1)^{a\cdot x}$ sign table (lines 249-258) and the concrete payoff $\text{amp}(101) = 8/8 = 1$ (lines 277-278): hidden string recovered with probability 1 in one query.
