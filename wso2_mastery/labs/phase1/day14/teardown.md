# Day 14 Lab — Teardown

## Nothing to stop

Day 14 is a log analysis lab.  No services were started.

The only file created is `log_samples/token_failure.log`, which is a static
synthetic log file.  No cleanup needed.

If you ran `grep` commands or opened the log file, simply close your editor.
